services:
  - type: web
    name: fastapi-pdf-api
    env: python
    buildCommand: |
      pip install playwright
      playwright install chromium
    startCommand: uvicorn api.main:app --host=0.0.0.0 --port=10000
    plan: free
